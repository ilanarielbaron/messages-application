import styled from "styled-components";

export const Wrapper = styled.div`
    display: flex;
    justify-content: center;

    h4 {
        font-weight: bold;
        white-space: nowrap;
        overflow: auto;
    }

    > div .MuiGrid-item {
        min-width: 250px;
    }
`
