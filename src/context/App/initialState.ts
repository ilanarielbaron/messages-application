import { Message } from "../../Api"

const initialState =
  [
    {
      messageList: [{ message: "initial message", priority: 0 }],
    },
    {
      getMessage: () => { }
    }
  ]


export default initialState;
