import React, { useState } from 'react';
import generateMessage, { Message } from '../../../Api';

export default function getMessage(dispatch: any) {
  return { message: "retornado", priority: 0 }
}
